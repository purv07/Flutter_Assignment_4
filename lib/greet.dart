import 'package:flutter/material.dart';

class greet extends StatelessWidget {
  const greet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Have a good day')),
    );
  }
}
